name: Täglicher BZKI-LMS Login

on:
  schedule:
    # 7:30 UTC bedeutet:
    # Im Sommer (MESZ) = 9:30 Uhr Start (läuft bis 12:30 Uhr)
    # Im Winter (MEZ) = 8:30 Uhr Start (läuft bis 11:30 Uhr)
    # So bist du IMMER sicher im Kernzeitfenster.
    - cron: '30 7 * * 1-5'
  workflow_dispatch: {}

jobs:
  login:
    runs-on: ubuntu-latest
    timeout-minutes: 200

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Abhängigkeiten installieren
        run: npm install playwright

      - name: Chromium installieren
        run: npx playwright install --with-deps chromium

      - name: Login-Skript ausführen
        env:
          LMS_USER: ${{ secrets.LMS_USER }}
          LMS_PASS: ${{ secrets.LMS_PASS }}
          SESSION_MINUTES: '180' # Bleibt bei 3 Stunden
        run: node login.js

      - name: Screenshots als Nachweis speichern
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: login-screenshots
          path: |
            *.png
          retention-days: 14
